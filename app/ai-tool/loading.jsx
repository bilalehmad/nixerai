export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <section className='w-full max-w-full h-screen flex justify-center mt-24'>Loading...</section>
    )
  }